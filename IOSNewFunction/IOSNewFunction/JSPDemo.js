
defineClass("ViewController", {
            
            btn1: function() {
            console.log('hello')
            var buttonColor = require('UIColor').redColor()
            self.button().setBackgroundColor(buttonColor)
            }
            }, {});
